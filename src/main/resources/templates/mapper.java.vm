#set($JavaMapperName = ${package.ModuleName} + "Mapper")
package com.kuo.storyboard.mapper;

import com.kuo.storyboard.entity.${package.ModuleName};
import ${superMapperClassPackage};

/**
 * @author ${author}
 * @date ${cfg.dateTime}
 * @describe $!{table.comment}Mapper类
 */
public interface ${JavaMapperName} extends ${superMapperClass}<${package.ModuleName}> {

}